
<% @category.sub_categories.each do |sub_category| %>
    <%= sub_category.name %>
    <%#= link_to 'Edit', edit_sub_category_path(sub_category), class: "btn btn-link" %>
    <%#= link_to 'Delete', sub_category_path(sub_category),
          method: :delete,
          data: { confirm: 'Are you sure?' }, class: "btn btn-link" %>
  <br />
<% end %>
